{
  "name": "gakushu-sochi-workspace",
  "private": true,
  "version": "0.0.1",
  "license": "UNLICENSED",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "compile": "npm run compile --workspace=@gakushu-sochi/domain && npm run compile --workspace=gakushu-sochi",
    "gen:concepts": "npm run gen:concepts --workspace=@gakushu-sochi/domain",
    "check:concepts": "npm run check:concepts --workspace=@gakushu-sochi/domain",
    "test": "npm run compile && npm run test:unit --workspace=gakushu-sochi",
    "test:unit": "npm run compile && npm run test:unit --workspace=gakushu-sochi",
    "lint": "npm run lint --workspace=gakushu-sochi",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "prettier": "^3.9.6"
  }
}
